#!/usr/bin/env bash
# Objective grading only. Exit non-zero on FAIL.
set -uo pipefail
# EXPECTED_FLAG_SHA256="..."   # store the hash, never the plaintext flag
echo "TODO: implement checks"
echo "FAIL"; exit 1
